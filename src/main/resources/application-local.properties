# Local: the app runs on the Mac, the database runs in Docker.
# Host is localhost:5433 - the port docker-compose publishes.
spring.datasource.url=jdbc:postgresql://localhost:5433/catalog_local
spring.datasource.username=catalog_local_user

logging.level.org.hibernate.SQL=DEBUG
